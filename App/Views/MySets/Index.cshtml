@using Core.DTOs
@using Core.Models
@model IEnumerable<SetDTO>

@{
    ViewData["Title"] = "Мої сети";
}

<style>
    .filter-container {
        display: flex;
        gap: 15px;
        margin-bottom: 30px;
        background: rgba(255, 255, 255, 0.03);
        padding: 15px;
        border-radius: 12px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        align-items: center;
    }

    .filter-form {
        display: flex;
        flex-grow: 1;
        gap: 12px;
    }

        .filter-form input, .sort-select {
            flex-grow: 1;
            background: #141B25 !important;
            border: 1px solid #818375 !important;
            color: white !important;
            padding: 10px 15px !important;
            border-radius: 8px !important;
            outline: none;
            height: 45px;
        }

    .btn-search {
        background: #f4a261 !important;
        color: #141B25 !important;
        border: none !important;
        padding: 0 25px !important;
        border-radius: 8px !important;
        cursor: pointer;
        font-weight: bold;
        height: 45px;
    }

    .sets-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 25px;
        margin-top: 20px;
    }

    .set-card {
        background-color: var(--btn-bg);
        border: 1px solid var(--btn-border);
        border-radius: 12px;
        padding: 25px;
        min-height: 190px;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        position: relative;
        cursor: pointer;
        transition: 0.3s ease;
    }

        .set-card:hover {
            border-color: var(--accent-color);
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.4);
        }

    .card-footer {
        margin-top: auto;
        padding-top: 10px;
        border-top: 1px solid rgba(240, 255, 255, 0.05);
    }

    .sets-count-badge {
        background: rgba(240, 255, 255, 0.1);
        color: var(--accent-color);
        padding: 2px 8px;
        border-radius: 6px;
        font-size: 0.75rem;
        font-weight: bold;
    }

    .date-info {
        font-size: 0.65rem;
        opacity: 0.4;
        display: flex;
        justify-content: space-between;
        margin-top: 8px;
        color: white;
    }

    .card-menu-btn {
        position: absolute;
        top: 15px;
        right: 15px;
        cursor: pointer;
        font-size: 1.5rem;
        opacity: 0.5;
        z-index: 20;
        padding: 5px;
    }

    .dropdown {
        position: absolute;
        top: 45px;
        right: 15px;
        background: #2C3440;
        border: 1px solid #818375;
        border-radius: 8px;
        display: none;
        flex-direction: column;
        z-index: 100;
        overflow: hidden;
        box-shadow: 0 10px 25px rgba(0,0,0,0.5);
    }

        .dropdown button, .dropdown form button {
            background: none;
            border: none;
            color: white;
            padding: 10px 20px;
            font-size: 0.9rem;
            cursor: pointer;
            text-align: left;
            width: 100%;
            display: block;
        }

            .dropdown button:hover, .dropdown form button:hover {
                background-color: #6B8789;
            }

    .modal-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.85);
        display: none;
        justify-content: center;
        align-items: center;
        z-index: 1000;
    }

    .modal {
        background: #1C2531;
        border: 1px solid var(--accent-color);
        padding: 20px;
        border-radius: 15px;
        width: 460px;
        display: flex;
        flex-direction: column;
        gap: 3px;
    }

    .field-block {
        display: flex;
        flex-direction: column;
        width: 100%;
        margin-bottom: 2px;
    }

        .field-block label {
            font-size: 0.75rem;
            opacity: 0.6;
            margin-bottom: 2px;
            color: white;
        }

    .modal input, .modal select, .modal textarea {
        background: #141B25;
        border: 1px solid #818375;
        color: white !important;
        padding: 10px;
        border-radius: 8px;
        outline: none;
        box-sizing: border-box;
        font-family: inherit;
        width: 100%;
    }

    .stepper {
        display: flex;
        align-items: center;
        background: #141B25;
        border: 1px solid #818375;
        border-radius: 8px;
        overflow: hidden;
        height: 42px;
    }

        .stepper button {
            background: none;
            border: none;
            color: var(--accent-color);
            width: 40px;
            height: 100%;
            cursor: pointer;
            font-size: 1.2rem;
        }

        .stepper input {
            width: 50px !important;
            border: none !important;
            text-align: center !important;
            padding: 0 !important;
            background: none !important;
            font-weight: bold;
            color: white !important;
        }

    input::-webkit-outer-spin-button, input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    .ai-preview-container {
        background: rgba(0,0,0,0.2);
        border: 1px solid #444;
        border-radius: 8px;
        padding: 8px;
        max-height: 120px;
        overflow-y: auto;
        display: none;
        margin-bottom: 5px;
    }

    .modal-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid #333;
        padding-top: 15px;
        margin-top: 10px;
    }

    .btn-ai {
        background: #f4a261;
        color: #141B25 !important;
        border: none;
        padding: 10px 20px;
        border-radius: 8px;
        font-weight: bold;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }

    .spinner-mini {
        width: 14px;
        height: 14px;
        border: 2px solid rgba(255,255,255,0.2);
        border-top-color: #fff;
        border-radius: 50%;
        animation: spin 0.8s linear infinite;
    }

    @@keyframes spin {
        to {
            transform: rotate(360deg);
        }
    }

    .switch {
        position: relative;
        display: inline-block;
        width: 40px;
        height: 20px;
    }

        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

    .slider {
        position: absolute;
        cursor: pointer;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: #444;
        transition: .4s;
        border-radius: 20px;
    }

        .slider:before {
            position: absolute;
            content: "";
            height: 14px;
            width: 14px;
            left: 3px;
            bottom: 3px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

    input:checked + .slider {
        background-color: #6B8789;
    }

        input:checked + .slider:before {
            transform: translateX(20px);
        }
</style>

<div class="content-area">
    <h2>Ваші навчальні сети</h2>

    <div class="filter-container">
        <form asp-controller="MySets" asp-action="Index" method="get" class="filter-form">
            <input type="text" name="searchText" placeholder="Пошук сетів..." value="@ViewData["CurrentFilter"]" />
            <select name="categoryId" class="sort-select" onchange="this.form.submit()">
                <option value="">Усі категорії</option>
                @if (ViewBag.Categories != null)
                {
                    foreach (var cat in (IEnumerable<CategoryDTO>)ViewBag.Categories)
                    {
                        <!option value="@cat.Id" @(ViewData["CurrentCategory"]?.ToString() == cat.Id.ToString() ? "selected" : "")>@cat.Name</!option>
                    }
                }
            </select>
            <button type="submit" class="btn-search">Шукати</button>
        </form>
    </div>

    <div class="sets-grid">
        <div class="set-card create-card" onclick="openCreateModal()">
            <div style="font-size: 3rem; color: var(--accent-color);">+</div>
            <div style="font-weight: 500;">Створити новий сет</div>
        </div>

        @foreach (var set in Model)
        {
            <div class="set-card" onclick="handleCardClick(event, '@Url.Action("Index", "Details", new { id = set.Id })')">
                <div class="card-menu-btn" onclick="toggleMenu(event)">⋮</div>
                <div class="dropdown" onclick="event.stopPropagation()">
                    <button type="button" onclick="openEditModal(event, '@set.Id', '@set.Name', '@set.Description', '@set.IsPublic'.toLowerCase(), '@((int)set.Type)')">Редагувати</button>
                    <form asp-action="Delete" method="post" onsubmit="return confirm('Видалити?')">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@set.Id" />
                        <button type="submit" style="color: #ff8080;">Видалити</button>
                    </form>
                </div>
                <h3 style="margin: 0; color: var(--accent-color);">@set.Name</h3>
                <p style="opacity: 0.7; font-size: 0.85rem; margin: 5px 0; flex-grow: 1;">@set.Description</p>
                <div class="card-footer">
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <span class="sets-count-badge">@set.FlashcardsCount карток</span>
                        @if (set.IsGenerated)
                        {
                            <span style="font-size: 0.7rem; color: #f4a261; font-weight: bold;">✨ ШІ</span>
                        }
                    </div>
                    <div class="date-info">
                        <span>Створено: @set.CreatedAt.ToLocalTime().ToString("dd.MM.yyyy")</span>
                        <span>@((int)set.Type == 0 ? "Мова" : "Предмет")</span>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

@* МОДАЛКА *@
<div class="modal-overlay" id="setModal">
    <div class="modal">
        <form id="setForm" method="post" action="">
            @Html.AntiForgeryToken()
            <h2 id="modalTitle" style="margin: 0 0 5px 0; font-size: 1.2rem;">Новий сет</h2>
            <input type="hidden" name="Id" id="setIdField">
            <input type="hidden" name="GeneratedCardsJson" id="generatedCardsJson">

            <div style="display: flex; gap: 10px; width: 100%;">
                <div class="field-block" style="flex: 2;">
                    <label>Тип навчання</label>
                    <select name="Type" id="setTypeField" onchange="toggleLanguageSelection()">
                        <option value="0">Мова (Language)</option>
                        <option value="1">Предмет (Subject)</option>
                    </select>
                </div>
                <div class="field-block" style="flex: 1;">
                    <label>Кількість</label>
                    <div class="stepper">
                        <button type="button" onclick="stepCount(-1)">−</button>
                        <input type="number" id="aiCount" value="5" min="1" max="30" readonly>
                        <button type="button" onclick="stepCount(1)">+</button>
                    </div>
                </div>
            </div>

            <div id="languageSelectionRow" style="display: none; align-items: center; gap: 10px; margin-bottom: 5px;">
                <div class="field-block" style="flex: 1;"><label>Вивчаю</label><select id="learnLangList" name="LearnLanguage"></select></div>
                <div style="margin-top: 15px; color: var(--accent-color);">→</div>
                <div class="field-block" style="flex: 1;"><label>На яку</label><select id="nativeLangList" name="NativeLanguage"></select></div>
            </div>

            <div class="field-block">
                <label>Назва сету</label>
                <input type="text" name="Name" id="setNameField" required placeholder="Назва..." />
            </div>

            <div class="field-block">
                <label>Опис (Промпт для ШІ)</label>
                <textarea name="Description" id="setDescField" rows="2" style="resize: none;" placeholder="Тема для ШІ..."></textarea>
            </div>

            <div id="aiPreviewArea" class="ai-preview-container"><div id="aiCardsList"></div></div>

            <div style="display: flex; justify-content: flex-end; align-items: center; gap: 10px; margin: 5px 0;">
                <span style="font-size: 0.8rem; opacity: 0.7;">Зробити публічним</span>
                <label class="switch"><input type="checkbox" name="IsPublic" id="setPublicField" checked value="true"><span class="slider"></span></label>
            </div>

            <div class="modal-footer">
                <button type="button" onclick="closeModal()" style="background: none; border: none; color: #818375; cursor: pointer; font-weight: bold;">Скасувати</button>
                <div style="display: flex; gap: 8px;">
                    <button type="button" id="btnAiGen" class="btn-ai" onclick="generateWithAI()">✨ ШІ</button>
                    <button type="submit" style="background: #6B8789; color: white; border: none; padding: 10px 25px; border-radius: 8px; font-weight: bold; cursor: pointer;">Зберегти</button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        const modal = document.getElementById('setModal');
        const setForm = document.getElementById('setForm');
        let tempCards = [];

        function stepCount(val) {
            const input = document.getElementById('aiCount');
            let res = parseInt(input.value) + val;
            if (res >= 1 && res <= 30) input.value = res;
        }

        function openCreateModal() {
            setForm.reset();
            setForm.action = "/MySets/Create";
            document.getElementById('modalTitle').innerText = "Новий сет";
            document.getElementById('aiPreviewArea').style.display = 'none';
            tempCards = [];
            toggleLanguageSelection();
            modal.style.display = 'flex';
        }

        function openEditModal(e, id, name, desc, isPublic, type) {
            e.stopPropagation();
            setForm.action = "/MySets/Edit";
            document.getElementById('modalTitle').innerText = "Редагувати сет";
            document.getElementById('setIdField').value = id;
            document.getElementById('setNameField').value = name;
            document.getElementById('setDescField').value = desc;
            document.getElementById('setPublicField').checked = (isPublic === 'true');
            document.getElementById('setTypeField').value = type;
            document.getElementById('aiPreviewArea').style.display = 'none';
            toggleLanguageSelection();
            modal.style.display = 'flex';
            closeAllDropdowns();
        }

        async function generateWithAI() {
            const promptInput = document.getElementById('setDescField').value;
            const count = document.getElementById('aiCount').value;
            const btn = document.getElementById('btnAiGen');
            if (!promptInput) return alert("Вкажіть тему!");

            btn.disabled = true; btn.innerHTML = '<span class="spinner-mini"></span>...';
            const formData = new FormData();
            formData.append("Prompt", promptInput);
            formData.append("Count", count);

            try {
                const response = await fetch('/api/generate-cards', { method: 'POST', body: formData });
                if (!response.ok) throw new Error();
                tempCards = await response.json();
                const listDiv = document.getElementById('aiCardsList'); listDiv.innerHTML = '';
                tempCards.forEach((c, i) => {
                    listDiv.innerHTML += `<div style="display:flex; justify-content:space-between; font-size:0.75rem; padding:4px; border-bottom:1px solid rgba(255,255,255,0.1);"><span><b>${c.term}</b> - ${c.definition}</span><span onclick="tempCards.splice(${i},1); generateWithAI.call();" style="color:#ff8080; cursor:pointer;">✕</span></div>`;
                });
                document.getElementById('aiPreviewArea').style.display = 'block';
                document.getElementById('generatedCardsJson').value = JSON.stringify(tempCards);
            } catch (e) { alert("Помилка ШІ. Перевірте ключ Gemini."); }
            finally { btn.disabled = false; btn.innerHTML = "✨ ШІ"; }
        }

        function toggleLanguageSelection() { document.getElementById('languageSelectionRow').style.display = document.getElementById('setTypeField').value === "0" ? 'flex' : 'none'; }
        function closeModal() { modal.style.display = 'none'; }
        function toggleMenu(e) { e.stopPropagation(); closeAllDropdowns(); e.currentTarget.nextElementSibling.style.display = 'flex'; }
        function closeAllDropdowns() { document.querySelectorAll('.dropdown').forEach(d => d.style.display = 'none'); }
        function handleCardClick(e, url) { if (!e.target.closest('.card-menu-btn') && !e.target.closest('.dropdown') && !e.target.closest('form')) window.location.href = url; }
        window.onclick = (e) => { if (!e.target.matches('.card-menu-btn')) closeAllDropdowns(); if (e.target === modal) closeModal(); };

        const languages = [ { code: "uk", name: "Українська" }, { code: "en", name: "Англійська" }, { code: "de", name: "Німецька" } ];
        document.addEventListener("DOMContentLoaded", () => {
            const l1 = document.getElementById('learnLangList'); const l2 = document.getElementById('nativeLangList');
            languages.forEach(l => { l1.add(new Option(l.name, l.code)); l2.add(new Option(l.name, l.code)); });
            l1.value = "en"; l2.value = "uk";
        });
    </script>
}